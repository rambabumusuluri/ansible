- name: user creation
  hosts: web
  become: yes
  vars_files:
    - ../common.yml
  tasks:
    - name: create users as required
      ansible.builtin.user:
        name: "{{ item.name }}"
        uid: "{{ item.uid }}"
        shell: /bin/bash
      loop: "{{ users }}"
